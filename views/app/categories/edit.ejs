<%- include("./../../html/open.ejs") %>

<%- include("./../../components/navbar.ejs") %>

<div class="container pt-5">
    <div class="row">
        <div class="col col-12 col-md-6 col-lg-6 mx-auto">
            <form action="/app/categorias/editar" method="post">
                <h4 class="h4 h4-responsive text-black-50 mb-5">Edição de categoria</h4>
                <p class="text-black-50">Modifique como achar necessário e clique em "pronto"</p>
                <input type="hidden" name="id" value="<%= category.id %>">
                <input type="text" name="categname" class="mb-2 form-control" autofocus required value="<%= category.title %>">
                <button class="btn btn-success btn-block">pronto</button>
            </form>
        </div>
    </div>
</div>

<%- include("./../../html/close.ejs") %>